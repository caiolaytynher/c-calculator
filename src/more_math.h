#ifndef MORE_MATH_H
#define MORE_MATH_H

#include <stdbool.h>
#include <stdlib.h>

int mm_gcd(int, int);
int mm_lcm(int, int);
bool mm_is_perfect_square(int);
int mm_find_higher_perfect_square(int);
int mm_find_square_complement(int, int);

#endif
